# Citrus Canker YOLO 项目规则

## 数据集结构

- 图片放在 `dataset/images/{train,val,test}`，标签放在 `dataset/labels/{train,val,test}`。
- 图片与标签必须同名配对；无病斑的负样本也要保留一个空的 `.txt` 标签。
- 类别固定为 `0: canker`，标注格式为 YOLO 检测格式：`class x_center y_center width height`。
- 同一片物理叶片的不同角度、旋转、距离照片必须全部放在同一个集合，不能跨 `train`、`val`、`test`。
- 允许保留重复或重压缩图片，但重复图与其原图必须位于同一集合。
- 完全未使用的 `test` 只用于最终评估，不根据 test 结果反复调参；一旦根据其结果修改数据或模型，必须降级为阶段性诊断集。
- 当前 `dataset/images/test` 已用于 train-4 错误分析，后续只作阶段性诊断。最终汇报前应在模型与阈值固定后，另建由新物理叶片组成的 `external_test` 并一次性评估。
- 重新拆分数据后，旧的 `labels/*.cache` 移入 `dataset/cache_archive/<日期说明>/`，让 YOLO 重新扫描标签；归档目录不参与训练。
- 被新标注替代或误放的标签移入 `dataset/label_archive/<日期说明>/`，不直接删除；归档目录不参与训练。

## 病斑标注口径

- 每个边界可分辨的独立病斑使用一个紧框；相互分离的病斑分开标注。
- 框覆盖连续可见的褐色或坏死病斑及其直接相连的黄化晕圈，不纳入周围健康叶片。
- 反光、水滴、污渍和无法确认的小白点不作为溃疡病斑标注。
- 辅助标注时可查看模型预测作为质量复核，但不得直接复制预测框作为人工真值。
- train、val、test 采用同一标注口径；口径变更时需在结果报告中披露。

## 数据变更验证

每次新增、移动或重新标注后，必须检查：

1. 每张图片都有同名标签，且没有孤立标签。
2. 所有类别编号均为 `0`，坐标均在 `0–1` 范围内。
3. 同一片叶片没有跨集合。
4. `data.yaml` 仍指向 `images/train`、`images/val`、`images/test`。

## 训练规则

- 数据划分发生变化后，从 `yolo11n.pt` 重新训练，不使用旧实验的 `last.pt` 续训。
- 每次实验使用新的 `name`，保留训练曲线和验证结果，避免覆盖旧实验。
- 优先增加新的物理叶片和不同拍摄背景，而不是只增加同一片叶片的旋转照片。

## 实验结果归档

- 筛选后的可复现实验结果放在 `results/<run-name>/`，包括指标、配置、关键曲线、混淆矩阵和代表性预测图。
- `runs/` 继续作为本地原始输出目录，不直接提交；模型权重通过 GitHub Release 单独发布，避免进入 Git 历史。
- 结果报告必须区分 val 与 test，不得只报告较高的 val 指标。
- 若发现标签遗漏或病斑定义不一致，必须在报告中披露；未经确认不直接修改原始标签。

## 第一阶段应用结构与边界

- 2026-09-05 用户收窄界面范围：只保留单图上传、病斑检测、数量与结果下载；不再开发病例复核、评估对比页面。旧代码保留，不删除数据或历史记录。
- 当前页面使用 `web/simple.html`、`web/simple.js`、`web/simple.css`；初版页面资源仅保留备查，不作为默认入口。面积定量属于单独的后续功能，必须展示真实分割轮廓并注明方法与局限，不可用检测框面积冒充病斑面积。

- `canker_workbench/` 放共享推理、固定阈值评估、复核记录和本地服务代码；`web/` 放本地页面静态资源；`tests/` 放不依赖训练的自动化测试。
- `app.py` 为本地页面入口，`evaluate.py` 为统一评估入口，`configs/workbench.json` 保存演示默认配置。默认仅监听 `127.0.0.1`，不公开部署、不自动下载权重。
- `workspace/` 是忽略 Git 的运行区：每次检测或评估创建独立编号目录；上传原图、预测图、CSV/JSON 报告和复核记录只存这里。不覆盖历史输出，不自动清理或删除。
- 人工复核是独立的待审记录，不写回 `dataset/labels/`，不自动判定空标签，也不自动把复核样本加入训练。
- 评估区分固定阈值下的病斑 TP/FP/FN、图片级阴性误报率与跨阈值 mAP；不可用固定阈值结果冒充 mAP。无标签图片不可当阴性图片。
- 默认 `960 / conf=0.55` 只是现有诊断集演示参数；统一评估默认使用 val，不自动在 test 选择阈值。当前 test 始终注明阶段性诊断；外部测试缺失时不得伪造或挪用数据。
- 叶片编号必须来自人工确认或明确来源，未知则保留未知；重复文件哈希检查不等于确认所有同叶图片无泄漏。
- 修改后至少运行 `python -m unittest discover -s tests -v`、`python validate_dataset.py`、`git diff --check`；交付前用本地权重做真实推理、评估及页面交互验证。
